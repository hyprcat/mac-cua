# Observation Checklist — Human-Eye Invariants

These cells passed every automated probe but await human visual confirmation. The automation cannot judge them:

- **Ghost cursor renders & looks correct** — visual judgment of the overlay cursor's appearance and position.
- **No sub-second focus flash** — only a person perceives a transient flash as the target app is driven in the background.

Check each box once you have eyeballed the linked screenshot (or watched the live run) and confirmed both invariants hold.

## Pending observations

- [ ] Safari · scroll — web-area scrolled; confirm no focus flash — [screenshot](out/safari_scroll.png)
- [ ] VS Code · click — ghost cursor over editor; confirm overlay renders — [screenshot](out/vscode_click.png)
